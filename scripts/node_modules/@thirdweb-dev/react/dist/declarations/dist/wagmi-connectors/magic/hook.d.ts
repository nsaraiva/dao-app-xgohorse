import { ConnectionInfo, MagicConnectorOptions } from "./connector";
export declare function useMagicLink({ options }: {
    options: MagicConnectorOptions;
}): {
    error: Error | null;
    isConnected: boolean;
    isConnecting: boolean;
    connect: (connectionInfo: ConnectionInfo) => Promise<void>;
};
